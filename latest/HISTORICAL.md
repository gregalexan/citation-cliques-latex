# Historical implementation

This directory contains superseded pipeline experiments.  It is retained only
for provenance and is not read by the canonical workflow.  Do not use values,
flags, tables, or figures from this directory in the manuscript.

Run `make reproduce` from the parent directory and use only
`results/revision-v1/`.
